name: barbary-coast-marine
static_sites:
  - name: web
    github:
      repo: YOUR_GITHUB_ORG/YOUR_REPO
      branch: main
      deploy_on_push: true
    source_dir: /
    index_document: index.html
    error_document: index.html
    routes:
      - path: /
    envs: []